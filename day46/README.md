
create a script that does this, with filename: '.env'
```
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```
- goals:
  - read along a list

- rate limit API.
  - spotify  returns Retry-After on a 429 HttpError
- walk through a playlist.
- 
