
be sure to 
```
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

- rate limit API.
  - spotify  returns Retry-After on a 429 HttpError
- walk through a playlist.
- 
